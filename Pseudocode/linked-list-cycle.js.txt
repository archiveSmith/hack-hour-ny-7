-create a function named 'hasCycle(head)'
  -READ head = node1 object
  -declare a variable called 'previousValue' and leave it undefined
  -iterate through the head param (for in loop)
    -READ key = value, next props
    -if key equals the string 'next' then...
      -reassing previousValue to be the current value in head param object
    -end if
  -end of iteration
-end of hasCycle function